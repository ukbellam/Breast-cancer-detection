# Breast-cancer-detection
This project implements a Random Forest Classifier to classify breast cancer tumors as malignant or benign using the Breast Cancer Wisconsin dataset from scikit-learn. The model's performance is evaluated using accuracy, precision, recall, F1-score, K-Fold Cross-Validation, and Hyperparameter Tuning with GridSearchCV.
